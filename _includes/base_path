{% comment %}
  Use only baseurl so links are relative and work on both localhost and production.
  Previously base_path was site.url + baseurl, which made nav links go to the live domain.
{% endcomment %}
{% assign base_path = site.baseurl %}